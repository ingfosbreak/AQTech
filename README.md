```bash
หลังจาก pull ก็ docker compose up --build สักรอบ หลังรอบแรกไม่ต้อง --build แล้ว
ใช้ compose up กับ down

คำสั่งสร้าง virtual env อันใหม่ไม่ต้องใช้ venv แล้ว
1. pip install poetry
2. poetry install --no-root (กดที่ root folder เลย)
3. ctrl + shift + p ใน vscode แล้วเลือก env อันใหม่จาก poetry ได้เลย

ลองเข้าหน้าเว็ป localhost:8000 ถ้าขึ้นหน้าเว็ปแปลว่า django ลงสำเร็จ
```

